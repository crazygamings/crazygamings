
struct Request {
    int glWidth;
    int glHeight;
    bool fake_test;
    //bullet track section
    bool is_bullet_track;
    bool enable_fov;
    float bt_scope_distance;//default 150
    float bt_noscope_distance;//default 60
    bool skip_bot;
    bool skip_hidden;
    bool skip_knocked;
    int fov_value;//fov size
    int fov_type;//0 is normal fov //1 is smart fov
    int aim_target;
    //memory futures
    bool safe_norecoil;
    float safe_norecoil_value;//default 0.1f
    bool small_croshair;
    float small_croshaie_value;//default 0.5f
    bool cam_view;
    float cam_view_value;//default 1.0f
    bool scope_zoom;
    float scope_zoom_value;//default 13.0f
    bool wide_view;
    float wide_view_value;//default 100.0f
    bool x_effect;
    float XEffect_size;//default 5000.0f
	float XEffect_stroke_size;//default 46.0f
    bool private_aimbot;
    int aim_when;//1 = on aiming 2 = on firing 3 = both
    float aim_speed;//default 10.0f
    bool aimbot_recoil_helper;
    float aimbot_recoil_helper_power;//default 3.0f
    bool is_tracking_car;
    int esp_delay_cpu;//default 2
    bool aimbot_100m;
    bool is_vehichles;//تطفي المركبات فقط
    bool is_items;//تطفي الايتمز بالكامل بدون القنابل
    bool speed_car;
    float speed_car_value;//default 120.0f
    bool EnableGud;
    bool GudUP;
    bool GudStand;
    bool GudDown;
};

